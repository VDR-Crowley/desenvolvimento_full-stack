const lista = [ 'Vandi', 'Vende', 'Vindi'];
let nome = 'Vando'

if (lista.includes(nome)) {
  console.log('Nome disponivel'+'!!'.repeat(2))
} else {
  console.log('Not name undefined'+'!!'.repeat(2));
}